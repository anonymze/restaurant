---
import { Image } from "astro:assets";
import PlateImg from "@/assets/images/plat.webp";
import { motion } from "framer-motion";
---

<section class="bg-white py-10 px-4 md:py-24">
	<div class="container mx-auto max-w-6xl">
		<div class="grid gap-16 md:grid-cols-12 items-center">
			<!-- Large Feature Image -->
			<motion.div
				client:load
				transition={{ duration: 0.5 }}
				initial="hidden"
				whileInView="visible"
				viewport={{ once: true, amount: 0.1 }}
				variants={{
					visible: { opacity: 1, x: 0 },
					hidden: { opacity: 0, x: -100 },
				}}
				className="md:col-span-7"
			>
				<div class="aspect-[4/3] overflow-hidden">
					<Image src={PlateImg} alt="Notre équipe en cuisine" class="w-full h-full object-cover" />
				</div>
			</motion.div>

			<!-- Content -->
			<div class="md:col-span-5 space-y-8">
				<div class="space-y-4">
					<div class="flex items-center gap-3">
						<div class="h-0.5 w-8 bg-primary" aria-hidden="true"></div>
						<span class="text-sm font-medium uppercase tracking-wider text-primary"> Notre passion </span>
					</div>
					<h2 class="text-3xl font-serif text-stone-800 leading-tight">
						Une passion pour la cuisine authentique depuis 2010
					</h2>
				</div>

				<div class="space-y-6 text-stone-600">
					<p class="leading-relaxed">
						Notre restaurant est né d'une passion partagée pour la cuisine authentique et les produits de
						qualité. Chaque jour, nous collaborons avec des agriculteurs locaux pour sélectionner les
						meilleurs ingrédients de saison.
					</p>
					<p class="leading-relaxed">
						Notre équipe de passionnés s'engage à créer des plats qui racontent une histoire, celle de notre
						terroir et de notre savoir-faire artisanal.
					</p>
				</div>

				<!-- Stats -->
				<div class="grid grid-cols-3 gap-4 pt-6 border-t border-stone-200">
					<div class="text-center">
						<div class="text-3xl font-serif text-primary mb-2">15+</div>
						<div class="text-sm text-stone-600">Producteurs locaux</div>
					</div>
					<div class="text-center">
						<div class="text-3xl font-serif text-primary mb-2">30+</div>
						<div class="text-sm text-stone-600">Plats signatures</div>
					</div>
					<div class="text-center">
						<div class="text-3xl font-serif text-primary mb-2">13</div>
						<div class="text-sm text-stone-600">Années d'expérience</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

